fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database — PostgreSQL (Neon)
sqlalchemy==2.0.35
psycopg2-binary==2.9.9   # FIX: was pymysql (MySQL driver) — project uses PostgreSQL

# Authentication
python-jose==3.3.0
passlib[bcrypt]==1.7.4

# Validation & Settings
pydantic==2.9.2
pydantic[email]          # required for EmailStr validation in user_schema.py
python-dotenv==1.0.1

# Optional but recommended
alembic==1.13.2
python-multipart==0.0.9  # required for OAuth2PasswordRequestForm (login endpoint)